#include <bits/stdc++.h>
using namespace std;
typedef long long ll;

void test_case(int test) {
    int n, m; cin >> n >> m;
    for (int i = 0; i < 300; i++) cout << '5'; cout << '\n';
    for (int i = 0; i < 299; i++) cout << '4'; cout << "5\n";
}

int32_t main() {
    ios::sync_with_stdio(false); cin.tie(NULL); cout.tie(NULL);
    test_case(1);
    return 0;
}
