#include <iostream>
#include <stack>
#include <vector>

using namespace std;

int main() {
	int l,n,cnt=0;
	cin>>l>>n;
	pair<int,int> a[d];
	for (int i=0;i<n;i++){
		int d,r;
		cin>>d>>r;
		a[i] = make_pair(max(0, d - r), min(l,d+r));
	}
	sort(a,a+n);
	
	int 
	for (int i=0;i<n;i++){
		if(counter<l){
			counter += 
			}
		}
