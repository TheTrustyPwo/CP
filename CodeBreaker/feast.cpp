#include <bits/stdc++.h>
using namespace std;
typedef long long ll;

int n, k, a[300005];

int32_t main() {
	ios::sync_with_stdio(false); cin.tie(NULL); cout.tie(NULL);
	cin >> n >> k;
	for (int i = 0; i < n; i++) cin >> a[i];
	
}
